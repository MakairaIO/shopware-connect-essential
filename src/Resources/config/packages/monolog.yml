monolog:
  channels: ["makaira_essential"]

  handlers:
    makairaConnectEssentialsHandler:
      type: rotating_file
      path: "%kernel.logs_dir%/makaira-essential_%kernel.environment%.log"
      level: error
      max_files: 10
      channels: ["makaira_essential"]
